// import { Router } from "express";
// import {
//   createPermission,
//   getPermission,
//   updatePermission,
//   deletePermission,
// } from "../controllers/permission.controller";
// import { authMiddleware } from "../middlewares/authMiddleware";

// const router = Router();

// router.post("/", authMiddleware(["create:permissions"]), createPermission);
// router.get("/:id", authMiddleware(["read:permissions"]), getPermission);
// router.put("/:id", authMiddleware(["update:permissions"]), updatePermission);
// router.delete("/:id", authMiddleware(["delete:permissions"]), deletePermission);

// export default router;
